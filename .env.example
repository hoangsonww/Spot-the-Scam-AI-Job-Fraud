# Backend Environment Variables
# This file should be copied to .env in the project root directory
# Example: cp .env.example .env

# Google Gemini API Key for chatbot functionality
# Get your API key from: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# Allowed CORS origins for the API (comma-separated)
SPOT_SCAM_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Optional: Use quantized transformer model (0=disabled, 1=enabled)
SPOT_SCAM_USE_QUANTIZED=0

# Python configuration
PYTHONUNBUFFERED=1

# MLflow tracking (if using)
MLFLOW_TRACKING_URI=file:///app/mlruns
